# PyTorch CPU-only (install first for smaller footprint)
# For GPU support, replace with: pip install torch --index-url https://download.pytorch.org/whl/rocm6.3
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Core RL Framework
stable-baselines3
gymnasium
sb3-contrib

# Data Processing
pandas
numpy
yfinance
stockstats

# Visualization
matplotlib
plotly
streamlit
seaborn

# Trading APIs (optional - only needed for paper trading)
alpaca-py

# Utilities
python-dotenv
pyyaml
tqdm
tensorboard
